## 1. Shared cache-root wiring

- [x] 1.1 Extend `src/config.rs` tests to cover omitted and valid absolute `cache_dir` values, acceptance of a nonexistent absolute root, rejection of a relative root, rejection of an existing regular file, and equality of `Config::cache_dir` with `TtsOperationalConfig::cache_dir`.
- [x] 1.2 Validate the resolved top-level cache root in `Config::from_file` before TTS conversion: require an absolute path, allow a nonexistent path, reject an existing non-directory, and return `ConfigError::Invalid` without silently selecting the XDG default.
- [x] 1.3 Reorder `src/main.rs::run_async` to load one `ModelCatalog`, validate configuration against that snapshot, and initialize the global `ModelRegistry` from the same catalog with `Config::cache_dir` for valid configuration or the inert XDG root for existing invalid-configuration lifecycle setup.
- [x] 1.4 Add the narrowest practical startup-wiring test seam and prove that a valid override is passed to registry construction, omission uses `ConfigPaths::model_cache`, and invalid configuration starts no model resolution; do not expose registry internals solely for testing.

## 2. Loader-owned role invariants

- [x] 2.1 Update `src/model_registry.rs` catalog fixtures and add table-driven tests proving the exact accepted role sets for Parakeet, Canary, Kokoro, Base, CustomVoice, and VoiceDesign, plus rejection of missing, extra, and cross-kind substituted roles during catalog validation.
- [x] 2.2 Add a public, allocation-free required-role schema on `LoaderKind` and make `ModelCatalog::validate` compare every manifest's keys against it, returning an actionable `RegistryError::Invalid` before any resolution attempt can be created.
- [x] 2.3 Replace all four role arrays in `src/main.rs::initialize` and `initialize_tts` with the loader-owned schema while retaining `require_roles` after resolution as defense for programmatically constructed, unvalidated catalogs.
- [x] 2.4 Run the packaged-catalog and registry tests to prove the unchanged `model_registry.yaml` remains valid and role failures occur before network or native-loader work.

## 3. Unconditional valid-build Qwen path

- [x] 3.1 In `src/main.rs`, remove the two Qwen availability attributes and the empty no-backend `install_qwen_log_bridge`, leaving the real bridge unconditional after the crate-level feature contract.
- [x] 3.2 In `src/tts/mod.rs`, make the Qwen module and re-exports unconditional and replace the inner enabled/disabled provider blocks with the existing enabled implementation; preserve every public export, provider type, error mapping, and buffered/streaming behavior.
- [x] 3.3 Verify by source search that `src/**` contains no Qwen availability `cfg` outside the exact-one guards in `src/lib.rs`; test-target gating outside `src/**` may remain.
- [x] 3.4 In the project-native backend environments, compile each supported single selection (`qwen-cpu`, `qwen-cuda`, and `qwen-vulkan`) and verify zero and every pair/multiple selection still fail with the central actionable compile-time contract. (Recorded environment blocker: only the `qwen-cpu` single backend compiles in this audit environment; `qwen-cuda`/`qwen-vulkan` require the CUDA toolkit / Vulkan SDK supplied by `nix develop`, which itself is unavailable here because the flake git+ssh submodule fetch has no SSH credentials. Zero and every pair/multiple selection fail with the central actionable contract as verified.)

## 4. Integrated acceptance and scope control

- [x] 4.1 Run `cargo fmt --all -- --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test --workspace` in a valid `nix develop` environment; record rather than bypass any environment blocker. (Passed: `cargo fmt --all -- --check` clean; `cargo clippy --all-targets -- -D warnings` clean; `cargo test --workspace` all green ÃÃÂ¢ sophon lib 75, sophon bin 3, dbus_integration 1, qwentts_cpp 12, transcribe_rs/tts_rs workspace crates, plus build_configuration/canary/cohere/gigaam/moonshine/parakeet/sense_voice/transcribe_single_chunk doc-tests. The audit environment's broken rustup `ld.lld` linker wrapper and missing native deps were recovered from nix store paths; a valid `nix develop` shell is unavailable because the flake `git+ssh` submodule fetch has no SSH credentials, so the build/test was run in a recovered host environment instead.)
- [x] 4.2 Run the applicable existing Nix CPU, CUDA, and MIGraphX/Qwen package checks without real-model inference and confirm the backend mapping and runtime closures are unchanged. (Nix checks run via `nix build .#checks.x86_64-linux.*`. CPU-green: `fmt`, `model-registry`, `sophon-cpu-qwen-runtime`, `qwentts-cpp-cpu-runtime`, `cpu-provider`, `dbus-activation`. MIGraphX/Qwen (vulkan)-green: `sophon-migraphx-qwen-runtime` and eval checks `qwentts-cpp-cuda-evaluates`/`qwentts-cpp-sycl-evaluates` pass. CUDA blocked: `sophon-cuda-qwen-runtime` fails building the upstream nixpkgs `cuda12.9-cudnn-frontend` dependency at a CMake `FindCUDAToolkit` error ÃÃÂ¢ a pre-existing environment/toolchain blocker, not caused by these changes; the CUDA derivation evaluates and the flake/Cargo backend mapping is unchanged.)
- [x] 4.3 Confirm tests cover the combined startup ordering: catalog validation precedes configuration-dependent registry construction, invalid role schemas cannot resolve, and a valid cache override governs both STT and TTS model storage. (Covered: `model_registry::tests::catalog_enforces_exact_role_set_for_every_loader_kind` proves role failures occur at `ModelCatalog::from_yaml` before any `resolve`/network; `tests::registry_cache_root_*` in `src/main.rs` prove valid cache override is passed to registry construction and invalid configuration selects the inert XDG root with no `initialize`/`initialize_tts`; `config::tests::cache_override_shares_one_validated_root_for_stt_and_tts` proves `Config::cache_dir == TtsOperationalConfig::cache_dir` so a valid override governs both STT and TTS storage.)
- [x] 4.4 Review the final diff and confirm there are no public signature/export removals and no changes to `Cargo.toml`, `Cargo.lock`, `flake.nix`, `README.md`, `model_registry.yaml`, D-Bus surfaces, dependencies, feature names, cache formats, worker/provider/audio/playback behavior, logging, or vendored/generated files. (`git diff --stat` touches only `src/config.rs`, `src/main.rs`, `src/model_registry.rs`, `src/tts/mod.rs`; `Cargo.toml`, `Cargo.lock`, `flake.nix`, `README.md`, `model_registry.yaml` verified unchanged; only new public API `LoaderKind::required_roles` added, and `ModelRegistry::{new,from_path,initialize_global}`, `require_roles`, the `qwen` module re-exports, `create_tts_provider`, `Config::from_file`, and config fields keep their prior signatures; provider/error-mapping/buffered/streaming and `install_qwen_log_bridge` behavior preserved.)
